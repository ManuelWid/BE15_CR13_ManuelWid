<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <div class="navbar-brand"><img src="{{ asset('pictures/fireworks.png') }}" alt="" width="40" height="40" /></div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link home" href="/">Home</a>
          </li>
          {% if app.user %}

            {% if app.user.roles[0] == "ROLE_ADMIN" %}
              <li class="nav-item">
                <a class="nav-link" href="/crud/create">Create</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/crud/">CRUD</a>
              </li>
            {% endif %}

            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li>
            
          {% else %}
            <li class="nav-item">
              <a class="nav-link" href="/register">Register</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
          {% endif %}
        </ul>
        <form action="{{ path('app_search') }}">
          <input type="search" name="cat" placeholder="music, theater, sport">
          <input type="submit" value="Go">
        </form>
      </div>
    </div>
</nav>